# Calculadora-py
É uma calculadora com interface gráfica usando o CustomTkinter(CTk) e Regular Expressions(re ou regex).
It is a calculator with a graphical interface using CustomTkinter (CTk) and Regular Expressions (re or regex).
